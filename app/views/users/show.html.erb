<h2><%= @user.username %></h2>

<h3><%= link_to 'Upload a New Patch', new_user_patch_path(@user) %></h3>

<h3>PATCHES:</h3>

<ul><% @user.patches.each do |p| %>
      <li><%= link_to p.name, user_patch_path(@user, p) %></li>
    <% end %></ul>
